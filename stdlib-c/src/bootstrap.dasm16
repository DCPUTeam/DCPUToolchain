; TODO: do we really still need the bootstrap besides the _start and .OUTPUT ?
.BOUNDARY

; Handles initially jumping into the main function
; that the user has provided.
.EXPORT _start
:_start
    SET A, A
	.OUTPUT INIT
	.OUTPUT CODE
